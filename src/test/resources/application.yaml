server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chapter97
    username: root
    password: wnsdud45@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
logging:
  config: classpath:logback-spring-local.xml

